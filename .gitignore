# TypeScript output produced beside source by the default compiler config.
src/**/*.js
src/**/*.js.map
src/**/*.d.ts
# Package build output and local dependencies.
lib/
node_modules/

node_modules/
lib/
yarn.lock
*.log
.DS_Store

# tsc sourcemap output — always in lib/, never src/.
# (.d.ts not ignored — hand-written type declarations like colors.d.ts
#  are legit src files.)
src/**/*.js.map
